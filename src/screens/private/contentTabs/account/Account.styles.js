import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    card: {
        marginBottom: 16,
        marginHorizontal: 8,
    },
    btnForm: {
        marginHorizontal: 24,
    },
    input: {
        marginBottom: 10,
        marginHorizontal: 16,
    },
});