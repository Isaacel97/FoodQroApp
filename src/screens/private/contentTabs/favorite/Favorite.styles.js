import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    card: {
        marginBottom: 16,
        marginHorizontal: 16,
        backgroundColor: 'white',
        borderColor: 'black',
        borderWidth: 2,
    },
});